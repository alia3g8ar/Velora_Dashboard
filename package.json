{
  "name": "velora-crm",
  "version": "1.0.0",
  "private": true,
  "description": "Velora CRM — React/Refine frontend + NestJS/GraphQL backend",
  "scripts": {
    "dev:frontend": "npm --prefix frontend run dev",
    "dev:backend": "npm --prefix backend run start:dev",
    "build": "npm run build:frontend && npm run build:backend",
    "build:frontend": "npm --prefix frontend run build",
    "build:backend": "npm --prefix backend run build",
    "lint:frontend": "npm --prefix frontend run lint",
    "lint:backend": "npm --prefix backend run lint",
    "typecheck": "node frontend/node_modules/typescript/bin/tsc -p frontend/tsconfig.json --noEmit && node backend/node_modules/typescript/bin/tsc -p backend/tsconfig.json --noEmit",
    "codegen": "npm --prefix frontend run codegen",
    "seed": "npm --prefix backend run seed:demo",
    "migration:run": "npm --prefix backend run migration:run",
    "migration:show": "npm --prefix backend run migration:show",
    "test:frontend": "npm --prefix frontend run test:run"
  }
}
